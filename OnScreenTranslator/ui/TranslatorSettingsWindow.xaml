<Window x:Class="OnScreenTranslator.ui.TranslatorSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TranslatorSettings}" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        DataContext="{StaticResource Loc}"
        Background="#2D2D2D" Foreground="White" ThemeMode="Dark"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1C1C1C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#15FFFFFF"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2,0,0,0" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#45FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SettingButton" >
            <Setter Property="Background" Value="#1C1C1C" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#15FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI" />
            <Setter Property="Padding" Value="5,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#353535" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#45FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 15">
            <TextBlock Text="{Binding TranslatorEngine}" Margin="0 0 0 5" Opacity="0.7"/>
            <ComboBox x:Name="ComBoxTranslator" Height="30" Background="#1C1C1C" SelectedValuePath="Tag" Focusable="False"
                      VerticalContentAlignment="Center" SelectionChanged="TranslatorChanged">
                <ComboBoxItem Content="Google Free Translator" Tag="GoogleFreeTranslator"/>
                <ComboBoxItem Content="Libre Translator" Tag="LibreTranslator"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 0 0 15">
            <TextBlock Text="{Binding EndpointURL}" Margin="0 0 0 5" Opacity="0.7"/>
            <TextBox x:Name="TxtEndpoint" Style="{StaticResource ModernTextBox}" Height="30"
                     Text="http://localhost:5000"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 15">
            <TextBlock Text="{Binding ApiKey}" Margin="0 0 0 5" Opacity="0.7"/>
            <TextBox x:Name="TxtApiKey" Style="{StaticResource ModernTextBox}" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="{Binding Save}" Click="Save_Click" Style="{StaticResource SettingButton}" Margin="0 0 90 0" Width="90" 
                    Height="32"/>
            <Button Content="{Binding Cancel}" Click="Cancel_Click" Style="{StaticResource SettingButton}" Width="90" 
                    Height="32"/>
        </StackPanel>
    </Grid>
</Window>